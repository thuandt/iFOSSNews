<% content_for :title,  @article.title %>

<div class="span8" style="width:100%">
  <h4><%= @article.title %></h4>
    <div class="thumbnail">
      <%= image_tag @article.photothumb.url %>
    </div>

  <p><%= @article.body %></p>

  <div class="form-actions">
    <%= link_to 'Quay lại', new_path, :class => 'btn'  %>
  </div>

</div>

